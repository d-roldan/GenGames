"use client";
import { FormEvent, useEffect, useState } from "react"; import { api } from "@/lib/api";
type Version = {platform: string; version: string; minimum_supported_version: string; latest_version: string};
export function Versions({token}: {token: string}) { const [items, setItems] = useState<Version[]>([]); const load = () => api<Version[]>("/admin/versions", token).then(setItems); useEffect(() => { void load(); }, [token]); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = Object.fromEntries(new FormData(event.currentTarget)); const platform = String(data.platform); await api(`/admin/versions/${platform}`, token, {method: "PUT", body: JSON.stringify({version: data.version, minimum_supported_version: data.minimum_supported_version, latest_version: data.latest_version})}); await load(); } return <><section className="panel"><h2>Registrar versión</h2><form className="inline-form" onSubmit={save}><select name="platform"><option>android</option><option>windows</option></select><input name="version" placeholder="Actual" required/><input name="minimum_supported_version" placeholder="Mínima" required/><input name="latest_version" placeholder="Última" required/><button className="primary">Guardar</button></form></section><section className="panel"><table><thead><tr><th>Plataforma</th><th>Actual</th><th>Mínima</th><th>Última</th></tr></thead><tbody>{items.map(item => <tr key={item.platform}><td><strong>{item.platform}</strong></td><td>{item.version}</td><td>{item.minimum_supported_version}</td><td>{item.latest_version}</td></tr>)}</tbody></table></section></>; }

