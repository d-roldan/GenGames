# Local Android releases

Place the current Android APK at `GenGames-Android-ARM64.apk`. APK files are
ignored by Git and are served by the backend update endpoint in development.

Do not update `latest_version` before this artifact has been fully copied and
verified. Follow [`docs/android-updates.md`](../docs/android-updates.md) for every
Android client publication.
